import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  basePath: "/susu-tech.github.io",
  assetPrefix: "/susu-tech.github.io",
  output: "export",
};

export default nextConfig;
